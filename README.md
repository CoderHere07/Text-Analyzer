A lightweight text utility web app built with Django and Bootstrap 5.

Users can paste any text and apply one or more cleaning options:
- Remove punctuation
- Remove spaces
- Remove extra lines

The cleaned output is displayed along with a live character count.

Built with:
- Python & Django — backend logic and URL routing
- Bootstrap 5 — responsive UI and styling
- HTML Django templates — frontend rendering
